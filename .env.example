# Supabase project URL (Settings -> API -> Project URL)
SUPABASE_URL=https://your-project-id.supabase.co

# Service role key (Settings -> API -> Project API keys -> Service role key)
# Keep this secret; do not expose it to the browser or commit it to source control.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
